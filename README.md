# Advance Trading App

This repository contains the advance trading app application setup using docker-compose and kubernetes manifests.

## Structure
- `frontend/`: Web interface
- `backend/`: Node.js Express API
- `db/`: Postgres initialization scripts
- `k8s/`: Kubernetes manifests

## Getting Started

Using Docker Compose:
```bash
docker-compose up -d --build
```
