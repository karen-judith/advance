apiVersion: v1
kind: ConfigMap
metadata:
  name: advance-config
  namespace: advance-trading
data:
  SMTP_HOST: "smtp.gmail.com"
  SMTP_PORT: "465"
  SMTP_SECURE: "true"
  ALLOWED_ORIGINS: "http://localhost:3000,https://advance-trading.example.com"
